apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: cloudmart
  labels:
    app: user-service
    component: backend
    managed-by: jenkins
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer